---
phase: 04-optimizer-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dvhub/modules/optimizer/adapter-registry.js
  - dvhub/modules/optimizer/adapters/eos.js
  - dvhub/modules/optimizer/adapters/emhass.js
  - dvhub/modules/optimizer/schemas/eos-response.json
  - dvhub/modules/optimizer/schemas/emhass-response.json
  - dvhub/modules/optimizer/schemas/canonical-plan.json
  - dvhub/test/optimizer-adapter-eos.test.js
  - dvhub/test/optimizer-adapter-emhass.test.js
  - dvhub/test/optimizer-adapter-registry.test.js
autonomous: true
requirements:
  - OPT-01
  - OPT-02
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "EOS adapter builds canonical input from a telemetry snapshot, validates EOS response against JSON schema, and normalizes output to canonical 15-min slot plans"
    - "EMHASS adapter builds canonical input from a telemetry snapshot, validates EMHASS response against JSON schema, and normalizes output to canonical 15-min slot plans"
    - "Adapter registry registers adapters by name and exposes them for lookup and health check"
    - "Schema validation rejects malformed optimizer responses with logged errors (SEC-02)"
    - "Health check logs a warning when detected optimizer version is not in the tested set (SEC-03)"
  artifacts:
    - path: "dvhub/modules/optimizer/adapters/eos.js"
      provides: "EOS adapter with buildInput, validateResponse, normalizeOutput, healthCheck"
      exports: ["createEosAdapter"]
    - path: "dvhub/modules/optimizer/adapters/emhass.js"
      provides: "EMHASS adapter with buildInput, validateResponse, normalizeOutput, healthCheck"
      exports: ["createEmhassAdapter"]
    - path: "dvhub/modules/optimizer/adapter-registry.js"
      provides: "Adapter registration, discovery, health check orchestration"
      exports: ["createAdapterRegistry"]
    - path: "dvhub/modules/optimizer/schemas/eos-response.json"
      provides: "Ajv JSON schema for EOS optimization response"
    - path: "dvhub/modules/optimizer/schemas/emhass-response.json"
      provides: "Ajv JSON schema for EMHASS response"
    - path: "dvhub/modules/optimizer/schemas/canonical-plan.json"
      provides: "Ajv JSON schema for canonical plan with 15-min slots"
  key_links:
    - from: "dvhub/modules/optimizer/adapters/eos.js"
      to: "dvhub/modules/optimizer/schemas/eos-response.json"
      via: "Ajv compile and validate"
      pattern: "ajv\\.compile|validate"
    - from: "dvhub/modules/optimizer/adapters/emhass.js"
      to: "dvhub/modules/optimizer/schemas/emhass-response.json"
      via: "Ajv compile and validate"
      pattern: "ajv\\.compile|validate"
    - from: "dvhub/modules/optimizer/adapter-registry.js"
      to: "dvhub/modules/optimizer/adapters/eos.js"
      via: "register and get by name"
      pattern: "register.*eos|get.*eos"
---

<objective>
Build the optimizer adapter infrastructure: adapter registry, EOS adapter, EMHASS adapter, JSON validation schemas, and corresponding tests. This establishes the pluggable adapter pattern for external optimizer communication with schema validation (SEC-02) and version pinning (SEC-03).

Purpose: Foundation layer that all subsequent optimizer plans depend on -- canonical plan format, validated adapter I/O, and registry for adapter discovery.
Output: Working adapter registry with two adapters (EOS, EMHASS), three JSON schemas, and comprehensive unit tests.
</objective>

<execution_context>
@/Users/christin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-optimizer-core/04-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From dvhub/modules/dv/providers/luox.js (provider adapter pattern):
```javascript
export function createLuoxProvider() {
  return {
    name: 'luox',
    registerLayout: { ... },
    interpretWrite(address, value) { ... },
    formatRegisters(state) { ... }
  };
}
```

From dvhub/modules/gateway/index.js (existing EOS/EMHASS state builders):
```javascript
// eosState() returns:
{
  measurement: {
    battery_soc: [soc / 100],
    battery_power: [batteryPowerW],
    grid_import_w: [gridImportW],
    grid_export_w: [gridExportW],
    pv_power: [pvTotalW],
    load_power: [loadPowerW]
  },
  victron: {
    grid_setpoint_w: gridSetpointW,
    min_soc_pct: minSocPct,
    self_consumption_w: selfConsumptionW
  },
  prices: epexPriceArray()
}

// emhassState() returns:
{
  soc_init: soc / 100,
  battery_power_w: batteryPowerW,
  pv_power_w: pvTotalW,
  load_power_w: selfConsumptionW,
  grid_power_w: gridTotalW,
  prices: epexPriceArray(),
  timestamps: [...]
}
```

EOS API endpoints (from research):
- PUT /v1/measurement/data -- push measurement
- POST /v1/prediction/update -- trigger prediction
- GET /v1/energy-management/optimization/solution -- get result
- GET /v1/health -- health/version check
Default port: 8503, tested version: 0.2.0

EMHASS API endpoints (from research):
- POST /action/dayahead-optim -- run optimization
- POST /action/naive-mpc-optim -- MPC optimization
Default port: 5000, tested version: 0.17.0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JSON schemas and adapter registry with tests</name>
  <files>
    dvhub/modules/optimizer/schemas/canonical-plan.json,
    dvhub/modules/optimizer/schemas/eos-response.json,
    dvhub/modules/optimizer/schemas/emhass-response.json,
    dvhub/modules/optimizer/adapter-registry.js,
    dvhub/test/optimizer-adapter-registry.test.js
  </files>
  <read_first>
    dvhub/modules/dv/providers/luox.js,
    dvhub/modules/optimizer/index.js,
    dvhub/modules/dv/index.js
  </read_first>
  <behavior>
    - Test: createAdapterRegistry() returns object with register, get, getAll, healthCheckAll methods
    - Test: registry.register(adapter) stores adapter by adapter.name
    - Test: registry.get('eos') returns the registered EOS adapter
    - Test: registry.get('nonexistent') returns undefined
    - Test: registry.getAll() returns array of all registered adapters
    - Test: registry.healthCheckAll() calls healthCheck() on each adapter and returns results map
    - Test: healthCheckAll() logs warning for adapters where version is not in testedVersions set (SEC-03)
    - Test: healthCheckAll() handles adapter healthCheck() rejection gracefully (returns { healthy: false, error })
    - Test: canonical-plan.json schema validates a well-formed plan with slots
    - Test: canonical-plan.json schema rejects plan missing required 'slots' array
    - Test: canonical-plan.json schema rejects slot missing 'start' or 'end' fields
  </behavior>
  <action>
    1. Create `dvhub/modules/optimizer/schemas/canonical-plan.json`:
       - type: object, required: ["optimizer", "slots", "createdAt", "runId"]
       - optimizer: string (e.g., "eos", "emhass")
       - runId: string (UUID)
       - createdAt: string (ISO 8601)
       - slots: array of objects, each with:
         - required: ["start", "end"]
         - start: string (ISO 8601), end: string (ISO 8601)
         - gridImportWh: number (default 0), gridExportWh: number (default 0)
         - batteryChargeWh: number (default 0), batteryDischargeWh: number (default 0)
         - targetSocPct: number (default 0), expectedProfitEur: number (default 0)
         - meta: object or null (additionalProperties: true)

    2. Create `dvhub/modules/optimizer/schemas/eos-response.json`:
       - type: object, required: ["result"]
       - result: array of objects, each with:
         - required: ["start_datetime", "end_datetime"]
         - start_datetime: string, end_datetime: string
         - Eauto_SoC_pro: number (EV SoC prediction)
         - EAuto_obj: { Wh_pro_Stunde: number }
         - Batteries: array of objects with optional SoC, charge/discharge fields
         - Home_appliance_*: optional fields
         - additionalProperties: true (EOS evolves, don't reject unknown fields)

    3. Create `dvhub/modules/optimizer/schemas/emhass-response.json`:
       - type: object
       - additionalProperties: true (EMHASS format not fully documented for standalone mode)
       - required: []  (permissive schema, tighten after container testing per research recommendation)
       - Properties we know about:
         - optim_status: string
         - P_PV: array of numbers (W), P_Load: array of numbers (W)
         - P_grid_pos: array of numbers (import W), P_grid_neg: array of numbers (export W)
         - P_batt: array of numbers (+ charge, - discharge)
         - SOC_opt: array of numbers (0-1 fraction)
         - timestamps: array of strings

    4. Create `dvhub/modules/optimizer/adapter-registry.js`:
       - export function createAdapterRegistry({ log } = {})
       - Internal Map for adapters by name
       - register(adapter): validates adapter has name, buildInput, validateResponse, normalizeOutput, healthCheck; stores by name
       - get(name): returns adapter or undefined
       - getAll(): returns Array.from(adapters.values())
       - async healthCheckAll(): iterates adapters, calls healthCheck(), catches errors, returns Map of { name -> { healthy, version, warning, error } }
       - When healthCheck returns { version }, check against adapter.testedVersions (Set). If version not in set, log.warn with message: `Optimizer ${name} version ${version} is untested (tested: ${[...testedVersions].join(', ')})`

    5. Write tests in `dvhub/test/optimizer-adapter-registry.test.js` using node:test with describe/it blocks. Use mock adapters (plain objects with the interface methods). Test SEC-03 version warning by providing a mock log and verifying log.warn was called.
  </action>
  <verify>
    <automated>cd /Volumes/My\ Shared\ Files/CODEX/DVhub/.claude/worktrees/nervous-robinson/dvhub && node --test test/optimizer-adapter-registry.test.js</automated>
  </verify>
  <acceptance_criteria>
    - dvhub/modules/optimizer/adapter-registry.js contains `export function createAdapterRegistry`
    - dvhub/modules/optimizer/adapter-registry.js contains `register(adapter)`, `get(name)`, `getAll()`, `healthCheckAll()`
    - dvhub/modules/optimizer/adapter-registry.js contains `log.warn` call with string `untested`
    - dvhub/modules/optimizer/schemas/canonical-plan.json contains `"required"` with `"slots"` and `"optimizer"`
    - dvhub/modules/optimizer/schemas/eos-response.json contains `"result"` as required property
    - dvhub/modules/optimizer/schemas/emhass-response.json exists and is valid JSON
    - dvhub/test/optimizer-adapter-registry.test.js exits 0
  </acceptance_criteria>
  <done>Adapter registry registers/retrieves adapters, healthCheckAll warns on untested versions (SEC-03), three JSON schemas exist and validate correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EOS and EMHASS adapters with tests</name>
  <files>
    dvhub/modules/optimizer/adapters/eos.js,
    dvhub/modules/optimizer/adapters/emhass.js,
    dvhub/test/optimizer-adapter-eos.test.js,
    dvhub/test/optimizer-adapter-emhass.test.js
  </files>
  <read_first>
    dvhub/modules/optimizer/adapter-registry.js,
    dvhub/modules/optimizer/schemas/eos-response.json,
    dvhub/modules/optimizer/schemas/emhass-response.json,
    dvhub/modules/optimizer/schemas/canonical-plan.json,
    dvhub/modules/gateway/index.js
  </read_first>
  <behavior>
    EOS adapter tests:
    - Test: createEosAdapter(config) returns object with name='eos', buildInput, validateResponse, normalizeOutput, healthCheck
    - Test: buildInput({soc:50, pvTotalW:3000, loadPowerW:800, gridImportW:0, gridExportW:2200, batteryPowerW:0, prices:[{ts:...,ct_kwh:5}]}) returns EOS measurement format with battery_soc=[0.5], pv_power=[3000]
    - Test: validateResponse with valid EOS result array returns { valid: true }
    - Test: validateResponse with missing 'result' key returns { valid: false, errors: [...] } (SEC-02)
    - Test: normalizeOutput converts EOS slots to canonical format with camelCase field names and correct types
    - Test: normalizeOutput generates runId (UUID) and createdAt (ISO string)
    - Test: healthCheck() fetches /v1/health and returns { healthy: true, version }
    - Test: healthCheck() returns { healthy: false, error } on fetch failure

    EMHASS adapter tests:
    - Test: createEmhassAdapter(config) returns object with name='emhass', buildInput, validateResponse, normalizeOutput, healthCheck
    - Test: buildInput converts snapshot to EMHASS format with soc_init as 0-1 fraction
    - Test: validateResponse accepts any object (permissive schema per research)
    - Test: normalizeOutput converts EMHASS arrays to canonical 15-min slot plan
    - Test: healthCheck() returns { healthy: true } on successful connection
  </behavior>
  <action>
    1. Create `dvhub/modules/optimizer/adapters/eos.js`:
       - import { createRequire } from 'node:module' -- use Ajv from Fastify's deps or create standalone instance
       - import Ajv from 'ajv' (already installed as Fastify dependency)
       - import { readFileSync } from 'node:fs'; import { join, dirname } from 'node:path'; import { fileURLToPath } from 'node:url'
       - Load eos-response.json schema using readFileSync + JSON.parse relative to __dirname (use fileURLToPath(import.meta.url))
       - export function createEosAdapter(config = {}):
         - const baseUrl = config.baseUrl || 'http://localhost:8503'
         - const timeoutMs = config.timeoutMs || 5000
         - const testedVersions = new Set(config.testedVersions || ['0.2.0'])
         - const ajv = new Ajv({ allErrors: true }); const validate = ajv.compile(eosSchema)
         - return { name: 'eos', testedVersions, buildInput, validateResponse, normalizeOutput, healthCheck }
         - buildInput(snapshot): return { measurement: { battery_soc: [snapshot.soc / 100], battery_power: [snapshot.batteryPowerW || 0], grid_import_w: [snapshot.gridImportW || 0], grid_export_w: [snapshot.gridExportW || 0], pv_power: [snapshot.pvTotalW || 0], load_power: [snapshot.loadPowerW || 0] }, victron: { grid_setpoint_w: snapshot.gridSetpointW || 0, min_soc_pct: snapshot.minSocPct || 10, self_consumption_w: snapshot.selfConsumptionW || 0 }, prices: snapshot.prices || [] }
         - validateResponse(raw): const valid = validate(raw); return valid ? { valid: true } : { valid: false, errors: validate.errors }
         - normalizeOutput(raw): return { optimizer: 'eos', runId: crypto.randomUUID(), createdAt: new Date().toISOString(), slots: raw.result.map(slot => ({ start: slot.start_datetime, end: slot.end_datetime, gridImportWh: Number(slot.Last_Wh_pro_Stunde || 0), gridExportWh: Number(slot.Einspeisung_Wh_pro_Stunde || 0), batteryChargeWh: Number(slot.Akku_Wh_Charge || 0), batteryDischargeWh: Number(slot.Akku_Wh_Discharge || 0), targetSocPct: Number(slot.Akku_SoC || 0) * 100, expectedProfitEur: Number(slot.Kosten_EUR || 0), meta: { eautoSocPct: slot.Eauto_SoC_pro } })) }
         - async healthCheck(): try { const res = await fetch(`${baseUrl}/v1/health`, { signal: AbortSignal.timeout(timeoutMs) }); if (!res.ok) return { healthy: false, error: `HTTP ${res.status}` }; const data = await res.json(); return { healthy: true, version: data.version || 'unknown' } } catch (err) { return { healthy: false, error: err.message } }

    2. Create `dvhub/modules/optimizer/adapters/emhass.js`:
       - Same structure as EOS adapter
       - export function createEmhassAdapter(config = {}):
         - const baseUrl = config.baseUrl || 'http://localhost:5000'
         - const timeoutMs = config.timeoutMs || 5000
         - const testedVersions = new Set(config.testedVersions || ['0.17.0'])
         - buildInput(snapshot): return { soc_init: (snapshot.soc || 0) / 100, battery_power_w: snapshot.batteryPowerW || 0, pv_power_w: snapshot.pvTotalW || 0, load_power_w: snapshot.loadPowerW || 0, grid_power_w: snapshot.gridTotalW || 0, prices: snapshot.prices || [], timestamps: snapshot.timestamps || [], costfun: 'profit' }
         - validateResponse(raw): return { valid: typeof raw === 'object' && raw !== null } (permissive per research, tighten after container testing)
         - normalizeOutput(raw): Convert EMHASS arrays to 15-min slots. EMHASS returns parallel arrays (P_PV, P_Load, P_grid_pos, P_grid_neg, P_batt, SOC_opt, timestamps). Map by index to slots: { start: timestamps[i], end: timestamps[i+1] || addMinutes(timestamps[i], 15), gridImportWh: Math.max(0, (raw.P_grid_pos?.[i] || 0)) * 0.25, gridExportWh: Math.abs(Math.min(0, (raw.P_grid_neg?.[i] || 0))) * 0.25, batteryChargeWh: Math.max(0, (raw.P_batt?.[i] || 0)) * 0.25, batteryDischargeWh: Math.abs(Math.min(0, (raw.P_batt?.[i] || 0))) * 0.25, targetSocPct: (raw.SOC_opt?.[i] || 0) * 100, expectedProfitEur: 0, meta: null }
         - async healthCheck(): try { const res = await fetch(baseUrl, { signal: AbortSignal.timeout(timeoutMs) }); return { healthy: res.ok, version: 'unknown' } } catch (err) { return { healthy: false, error: err.message } }

    3. Write tests. Use mock fetch by saving/restoring global.fetch. Test buildInput/validateResponse/normalizeOutput as pure functions. Test healthCheck with mocked fetch. For SEC-02 test: call validateResponse with { notResult: [] } and assert valid === false.
  </action>
  <verify>
    <automated>cd /Volumes/My\ Shared\ Files/CODEX/DVhub/.claude/worktrees/nervous-robinson/dvhub && node --test test/optimizer-adapter-eos.test.js test/optimizer-adapter-emhass.test.js</automated>
  </verify>
  <acceptance_criteria>
    - dvhub/modules/optimizer/adapters/eos.js contains `export function createEosAdapter`
    - dvhub/modules/optimizer/adapters/eos.js contains `AbortSignal.timeout`
    - dvhub/modules/optimizer/adapters/eos.js contains `randomUUID`
    - dvhub/modules/optimizer/adapters/eos.js contains `validateResponse` that uses Ajv
    - dvhub/modules/optimizer/adapters/emhass.js contains `export function createEmhassAdapter`
    - dvhub/modules/optimizer/adapters/emhass.js contains `soc_init`
    - dvhub/test/optimizer-adapter-eos.test.js contains test for `valid: false` (SEC-02)
    - dvhub/test/optimizer-adapter-eos.test.js exits 0
    - dvhub/test/optimizer-adapter-emhass.test.js exits 0
  </acceptance_criteria>
  <done>EOS adapter translates snapshot to EOS format and normalizes response to canonical plan. EMHASS adapter does equivalent. Both validate responses against JSON schemas. All tests pass.</done>
</task>

</tasks>

<verification>
- All 3 test files pass: `cd dvhub && node --test test/optimizer-adapter-*.test.js`
- JSON schemas are valid: `node -e "JSON.parse(require('fs').readFileSync('dvhub/modules/optimizer/schemas/canonical-plan.json'))"` exits 0
- Adapter registry exports createAdapterRegistry
- EOS adapter exports createEosAdapter
- EMHASS adapter exports createEmhassAdapter
</verification>

<success_criteria>
- Adapter registry can register, retrieve, and health-check adapters
- EOS adapter builds correct input format matching existing eosState() pattern
- EMHASS adapter builds correct input format matching existing emhassState() pattern
- Schema validation rejects malformed responses (SEC-02)
- Version check warns on untested versions (SEC-03)
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/04-optimizer-core/04-01-SUMMARY.md`
</output>
