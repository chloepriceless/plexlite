---
phase: 02-guided-setup-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dv-control-webapp/public/setup.html
  - dv-control-webapp/public/setup.js
  - dv-control-webapp/test/setup-wizard.test.js
autonomous: true
requirements:
  - SET-01
  - SET-03
user_setup: []
must_haves:
  truths:
    - "First-run setup behaves like a real step-by-step flow instead of showing all major setup sections at once on one page."
    - "A user can move backward and forward through the early setup steps without losing the values already entered on previous screens."
    - "When a required setup value is missing or invalid, the wizard shows blocking feedback and stops the user from progressing until it is corrected."
  artifacts:
    - path: "dv-control-webapp/public/setup.js"
      provides: "Pure wizard-state and validation helpers plus a thin DOM adapter for the guided setup flow"
      min_lines: 250
    - path: "dv-control-webapp/public/setup.html"
      provides: "Stable setup wizard mount points and step/navigation hooks that later plans can render into without returning to the old four-card layout"
      contains: "setup"
    - path: "dv-control-webapp/test/setup-wizard.test.js"
      provides: "Wave 0 coverage for setup state, step transitions, validation gates, and cwd-independent helper loading"
      contains: "createSetupWizardState"
  key_links:
    - from: "dv-control-webapp/public/setup.js"
      to: "dv-control-webapp/test/setup-wizard.test.js"
      via: "Helpers are exposed for `node:test` so state and validation logic can be exercised without the browser DOM"
      pattern: "PlexLiteSetupWizard|globalThis"
    - from: "dv-control-webapp/public/setup.js"
      to: "dv-control-webapp/public/setup.html"
      via: "Wizard bootstrap and navigation logic target the setup shell mount points and step controls"
      pattern: "getElementById\\(|querySelector\\("
---

<objective>
Extract the state-management backbone for the new setup wizard so the first-run flow can behave like a true sequence instead of a static multi-card form.

Purpose: Land the Wave 0 testing surface and the draft/validation primitives that all later setup steps, transport branching, and review/save behavior will depend on.
Output: Testable wizard-state helpers, blocking validation primitives, and setup shell hooks ready for one-step-at-a-time rendering.
</objective>

<execution_context>
@/Users/christin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guided-setup-rebuild/02-RESEARCH.md
@.planning/phases/02-guided-setup-rebuild/02-VALIDATION.md
@dv-control-webapp/public/setup.html
@dv-control-webapp/public/setup.js
@dv-control-webapp/test/settings-shell.test.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Bootstrap Wave 0 setup-wizard tests with cwd-independent helper loading</name>
  <files>dv-control-webapp/test/setup-wizard.test.js, dv-control-webapp/public/setup.js</files>
  <action>Create the focused `node:test` harness required by `02-VALIDATION.md` and load `public/setup.js` through a cwd-independent path strategy based on `import.meta.url` instead of the brittle pattern called out in research. Expose a minimal pure helper surface on `globalThis` so the tests can cover wizard-state creation, step transitions, and validation results without depending on live DOM rendering.</action>
  <verify><automated>node --test dv-control-webapp/test/setup-wizard.test.js</automated></verify>
  <done>The repository has a fast setup-wizard test file that runs from the repo root and proves the first extracted setup helpers are callable outside the browser.</done>
</task>

<task type="auto">
  <name>Task 2: Replace DOM-owned setup state with a draft-config wizard model</name>
  <files>dv-control-webapp/public/setup.js</files>
  <action>Refactor `setup.js` so the source of truth becomes a wizard state object containing `draftConfig`, `effectiveConfig`, `meta`, `activeStepId`, ordered step metadata, visited/completed tracking, and per-step/per-field validation. Preserve the raw config shape expected by `/api/config`, reuse the existing load payload, and keep the DOM adapter thin so later plans can mount or unmount steps without losing values. Do not invent a setup-only DTO or rely on hidden fields staying in the DOM.</action>
  <verify><automated>node --check dv-control-webapp/public/setup.js && node --test dv-control-webapp/test/setup-wizard.test.js</automated></verify>
  <done>`setup.js` can build, update, and validate wizard state independently of the rendered form, and previous step values survive navigation changes.</done>
</task>

<task type="auto">
  <name>Task 3: Add setup shell hooks for sequential navigation and validation messaging</name>
  <files>dv-control-webapp/public/setup.html, dv-control-webapp/public/setup.js</files>
  <action>Reshape the static setup markup so it has explicit wizard shell hooks for step status, one active-step workspace, next/back controls, and a dedicated validation summary area, while keeping the current save/import controls available for later wiring. The goal here is structural readiness for a real wizard, not the full transport-specific UI yet, so use stable IDs/classes and keep the first plan focused on the state contract.</action>
  <verify><automated>rg -n "setup-(wizard|steps|workspace|nav|errors)" dv-control-webapp/public/setup.html dv-control-webapp/public/setup.js && node --test dv-control-webapp/test/setup-wizard.test.js</automated><manual>Inspect `dv-control-webapp/public/setup.html` and confirm the old always-open four-card layout has been reduced to shell hooks for one-step-at-a-time rendering plus forward/back navigation.</manual></verify>
  <done>The setup page exposes the structural hooks required for a sequential wizard and a visible place for blocking validation feedback.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check dv-control-webapp/public/setup.js`
- [ ] `node --test dv-control-webapp/test/setup-wizard.test.js`
- [ ] Setup state persists previous entries across step changes without reading hidden DOM fields as the long-term source of truth
- [ ] The page contains explicit wizard shell hooks for active step rendering, navigation, and validation summaries
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has a testable wizard-state foundation that satisfies the Wave 0 validation requirements for helper extraction, fast feedback, and blocking validation primitives
  </success_criteria>

<output>
After completion, create `.planning/phases/02-guided-setup-rebuild/02-01-SUMMARY.md`
</output>
