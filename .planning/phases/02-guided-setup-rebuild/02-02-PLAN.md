---
phase: 02-guided-setup-rebuild
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - dv-control-webapp/config-model.js
  - dv-control-webapp/public/setup.html
  - dv-control-webapp/public/setup.js
  - dv-control-webapp/public/styles.css
  - dv-control-webapp/test/setup-wizard.test.js
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
user_setup: []
must_haves:
  truths:
    - "First-run setup renders as a readable one-step-at-a-time wizard with only a small, beginner-focused subset of fields visible on each screen."
    - "The transport step derives its branch from `draftConfig.victron.transport` and shows different required fields for Modbus and MQTT, including the MQTT portal/broker-or-host nuance from research."
    - "Forward navigation is blocked by actionable inline validation instead of relying on backend normalization warnings or HTML input attributes alone."
  artifacts:
    - path: "dv-control-webapp/config-model.js"
      provides: "Canonical setup step metadata or field-group hints so setup branching stays aligned with the schema-driven config model"
      contains: "getConfigDefinition"
    - path: "dv-control-webapp/public/setup.js"
      provides: "Transport-aware rendering, field visibility, and per-step validation logic for the guided setup flow"
      min_lines: 300
    - path: "dv-control-webapp/public/setup.html"
      provides: "Wizard stepper markup and step-specific containers for basics, transport, DV/meter, and services"
      contains: "setup-step"
    - path: "dv-control-webapp/public/styles.css"
      provides: "Sequential wizard layout, inline validation treatment, and responsive step navigation styling"
      contains: "setup-"
    - path: "dv-control-webapp/test/setup-wizard.test.js"
      provides: "Coverage for transport-specific field sets, required-field gates, and beginner step ordering"
      contains: "mqtt"
  key_links:
    - from: "dv-control-webapp/config-model.js"
      to: "dv-control-webapp/public/setup.js"
      via: "Setup step rendering and field grouping stay anchored to schema metadata instead of a duplicate hard-coded path map"
      pattern: "getConfigDefinition|definition\\."
    - from: "dv-control-webapp/public/setup.js"
      to: "dv-control-webapp/public/styles.css"
      via: "Rendered step, error, and progress classes rely on the wizard styling hooks"
      pattern: "setup-"
---

<objective>
Turn the setup skeleton into a real beginner-facing wizard that changes fields and guidance based on the selected Victron transport.

Purpose: Deliver the visible guided flow required by Phase 2 while keeping the field inventory aligned with the config schema and enforcing meaningful validation before the user advances.
Output: One-step-at-a-time setup screens, transport-aware field branching, inline validation, and responsive wizard styling backed by fast helper tests.
</objective>

<execution_context>
@/Users/christin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guided-setup-rebuild/02-RESEARCH.md
@.planning/phases/02-guided-setup-rebuild/02-VALIDATION.md
@dv-control-webapp/config-model.js
@dv-control-webapp/public/setup.html
@dv-control-webapp/public/setup.js
@dv-control-webapp/public/styles.css
@dv-control-webapp/test/setup-wizard.test.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Encode wizard step ownership without duplicating config paths</name>
  <files>dv-control-webapp/config-model.js, dv-control-webapp/public/setup.js</files>
  <action>Extend the schema-driven metadata so setup can ask the config model which beginner-facing fields belong to the basics, transport, DV/meter, and services steps, including any transport-specific visibility or copy hints required by research. Keep `config-model.js` as the canonical place for defaults and field paths; do not create a separate hard-coded setup field registry in the frontend that can drift from the real config definition.</action>
  <verify><automated>node --input-type=module -e "import('./dv-control-webapp/config-model.js').then(({ getConfigDefinition }) => { const definition = getConfigDefinition(); if (!definition || !definition.fields || !definition.fields.length) process.exit(1); })" && node --test dv-control-webapp/test/setup-wizard.test.js</automated></verify>
  <done>The setup wizard can derive its step inventory from schema-backed metadata rather than an ad hoc frontend-only map.</done>
</task>

<task type="auto">
  <name>Task 2: Render one focused step at a time with transport-aware field branching</name>
  <files>dv-control-webapp/public/setup.html, dv-control-webapp/public/setup.js, dv-control-webapp/public/styles.css</files>
  <action>Implement the sequential stepper UI so only one setup step is active at a time, with visible progress, previous/next controls, and compact step-level layouts. In the transport step, derive the visible field set from `draftConfig.victron.transport`: Modbus must emphasize host/port/unitId/timeout, MQTT must emphasize portal ID, keepalive, and the broker-or-host fallback, and fields irrelevant to the chosen path should leave the screen. Keep beginner-focused helper copy concise and explain why each field matters without drifting into expert-only settings that belong in later Settings work.</action>
  <verify><automated>node --check dv-control-webapp/public/setup.js && node --test dv-control-webapp/test/setup-wizard.test.js</automated><manual>Load `/setup.html` with `needsSetup` true and confirm the wizard advances step by step, shows only a small focused field set per screen, and swaps the transport-specific fields when switching between Modbus and MQTT.</manual></verify>
  <done>The setup flow is visibly sequential, transport-aware, and substantially calmer than the old four-card page.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce actionable step-level validation with focused coverage</name>
  <files>dv-control-webapp/public/setup.js, dv-control-webapp/test/setup-wizard.test.js, dv-control-webapp/public/styles.css</files>
  <action>Implement the validation rules from `02-RESEARCH.md` so advancing from each step requires the right inputs for that branch: HTTP/network basics, Modbus requirements, MQTT portal/broker-or-host rules, DV/meter essentials, and conditional service requirements for EPEX and Influx. Render inline error states and summary messaging that explain what to fix next, and expand the helper tests to cover both valid and invalid paths without depending on manual browser interaction.</action>
  <verify><automated>node --test dv-control-webapp/test/setup-wizard.test.js</automated></verify>
  <done>The wizard blocks invalid forward progress with clear, branch-specific feedback and automated tests guard the most important required-field rules.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check dv-control-webapp/public/setup.js`
- [ ] `node --test dv-control-webapp/test/setup-wizard.test.js`
- [ ] The setup flow shows one active step at a time with visible progress and back/next controls
- [ ] Modbus and MQTT produce different field sets and different validation requirements on the transport step
- [ ] Invalid required inputs stop forward navigation and show actionable error text instead of silently defaulting
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Guided setup now behaves like a real sequential wizard with transport-aware field branching and explicit validation gates
  </success_criteria>

<output>
After completion, create `.planning/phases/02-guided-setup-rebuild/02-02-SUMMARY.md`
</output>
