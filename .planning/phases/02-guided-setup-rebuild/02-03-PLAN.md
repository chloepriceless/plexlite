---
phase: 02-guided-setup-rebuild
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - dv-control-webapp/public/setup.html
  - dv-control-webapp/public/setup.js
  - dv-control-webapp/public/styles.css
  - dv-control-webapp/test/setup-wizard.test.js
autonomous: true
requirements:
  - SET-03
  - SET-04
user_setup: []
must_haves:
  truths:
    - "The final setup step presents a review of the key saved values and important effective/defaulted outcomes before persistence, so the user can confirm the real configuration result."
    - "Entering the review step and clicking save both run full-draft validation, preventing invalid required inputs from slipping through to `/api/config`."
    - "Save and import outcomes surface restart-required consequences and backend normalization warnings clearly before redirecting to Settings."
  artifacts:
    - path: "dv-control-webapp/public/setup.js"
      provides: "Review-step summary generation, full-draft validation, save/import integration, and post-save warning/restart messaging"
      min_lines: 320
    - path: "dv-control-webapp/public/setup.html"
      provides: "Review-step summary container and final action area for save/import completion messaging"
      contains: "review"
    - path: "dv-control-webapp/public/styles.css"
      provides: "Review-state summary, warning, and save-outcome presentation for the setup wizard"
      contains: "setup-review"
    - path: "dv-control-webapp/test/setup-wizard.test.js"
      provides: "Coverage for review snapshot generation, full-draft validation, and save-warning/restart message behavior"
      contains: "review"
  key_links:
    - from: "dv-control-webapp/public/setup.js"
      to: "dv-control-webapp/public/setup.html"
      via: "Review rendering and save state target dedicated summary, warning, and action containers"
      pattern: "review|summary|warning"
    - from: "dv-control-webapp/public/setup.js"
      to: "/api/config"
      via: "Final save path uses the existing config API while surfacing `restartRequired`, `restartRequiredPaths`, and returned warnings"
      pattern: "apiFetch\\('/api/config|apiFetch\\('/api/config/import"
---

<objective>
Finish Phase 2 by adding a review-and-save stage that validates the whole setup draft, summarizes the important resulting values, and explains save consequences clearly.

Purpose: Close the guided setup loop so users can confirm key choices before persisting them and understand what happens after save, especially for restart-sensitive transport changes.
Output: Review step, final validation gate, save/import feedback, and helper coverage for review generation and save-result messaging.
</objective>

<execution_context>
@/Users/christin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guided-setup-rebuild/02-RESEARCH.md
@.planning/phases/02-guided-setup-rebuild/02-VALIDATION.md
@dv-control-webapp/public/setup.html
@dv-control-webapp/public/setup.js
@dv-control-webapp/public/styles.css
@dv-control-webapp/server.js
@dv-control-webapp/test/setup-wizard.test.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build the review snapshot from raw draft and effective outcomes</name>
  <files>dv-control-webapp/public/setup.js, dv-control-webapp/public/setup.html, dv-control-webapp/public/styles.css</files>
  <action>Add a final review step that derives its summary from both the raw draft config and the effective/defaulted config returned by the backend load path, so users can see the key values that will actually matter after save. Include the transport choice, core connection values, DV/meter essentials, services toggles, and concise notes for inherited/defaulted outcomes where research says the sparse raw payload alone would be misleading.</action>
  <verify><automated>node --test dv-control-webapp/test/setup-wizard.test.js</automated><manual>Run the wizard for both Modbus and MQTT, open the review step, and confirm it summarizes the key values plus the most important inherited/defaulted outcomes clearly.</manual></verify>
  <done>The wizard has a meaningful review step instead of jumping straight from the last form screen to save.</done>
</task>

<task type="auto">
  <name>Task 2: Gate review and save behind full-draft validation</name>
  <files>dv-control-webapp/public/setup.js, dv-control-webapp/test/setup-wizard.test.js</files>
  <action>Run a full-draft validation pass before the wizard enters review and again immediately before calling `/api/config` or `/api/config/import`. Reuse the same validation model from earlier steps so invalid required inputs remain visible with actionable feedback, and make sure the save path cannot succeed in the UI when blocking errors still exist.</action>
  <verify><automated>node --check dv-control-webapp/public/setup.js && node --test dv-control-webapp/test/setup-wizard.test.js</automated></verify>
  <done>The review step and final save action are both protected by explicit validation gates, satisfying the final setup-validation requirement instead of trusting backend normalization.</done>
</task>

<task type="auto">
  <name>Task 3: Surface save consequences, warnings, and redirect behavior cleanly</name>
  <files>dv-control-webapp/public/setup.js, dv-control-webapp/public/setup.html, dv-control-webapp/public/styles.css, dv-control-webapp/test/setup-wizard.test.js</files>
  <action>Finish the save/import integration so the wizard shows backend warning lists, restart-required consequences, and next-step messaging before redirecting to Settings. Use the existing response fields (`restartRequired`, `restartRequiredPaths`, and returned warnings) rather than inventing a second protocol, and keep the completion copy clear about what is saved now versus what only becomes active after restart or reconnect.</action>
  <verify><automated>node --test dv-control-webapp/test/setup-wizard.test.js dv-control-webapp/test/settings-shell.test.js</automated><manual>Save a transport change and confirm the completion state explains restart-sensitive consequences before redirecting to `/settings.html?setup=done`.</manual></verify>
  <done>Setup completion messaging matches the backend save semantics and leaves the user with a clear understanding of what to do next.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check dv-control-webapp/public/setup.js`
- [ ] `node --test dv-control-webapp/test/setup-wizard.test.js dv-control-webapp/test/settings-shell.test.js`
- [ ] The review step summarizes the most important setup values and effective outcomes before final save
- [ ] Full-draft validation blocks entry into review and blocks final save until required errors are fixed
- [ ] Save/import results explain warnings, restart-sensitive transport consequences, and redirect to Settings cleanly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Guided setup ends with a trustworthy review-and-save experience that satisfies the phase goal for validation and final confirmation
  </success_criteria>

<output>
After completion, create `.planning/phases/02-guided-setup-rebuild/02-03-SUMMARY.md`
</output>
